1. This project is managed by uv (pyproject.toml).
2. All commands should be run via uv (e.g. uv run pytest, etc). Do this directly without changing directory.
3. Precommit hooks are configured in .pre-commit-config.yaml and should be run after major changes.
4. STRICTLY FOLLOW the coding conventions and architectural patterns defined in docs/CONVENTIONS.md.
